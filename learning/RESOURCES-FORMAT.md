# RESOURCES-FORMAT

A flat, ordered list of links and pointers to source material that lessons cite. Group by trust tier (primary / secondary / tertiary / community), not by topic — topic-grouping happens inside lessons.

Each entry should answer: *what is this, and when do I reach for it?*
